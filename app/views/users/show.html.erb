<% content_for :page_title, "User Info" %>

<section class="section">
	<div class="container">
		<div class="card">
		  <!-- <header class="card-header">
		    <p class="card-header-title">
		      Component
		    </p>
		    <a href="#" class="card-header-icon" aria-label="more options">
		      <span class="icon">
		        <i class="fas fa-angle-down" aria-hidden="true"></i>
		      </span>
		    </a>
		  </header> -->
			<div class="card-content">
			    <div class="content">
			      	<h3 class="card-title">
						<%= "#{@user.firstname} #{@user.lastname}" %>
					</h3>
			    </div>
			</div>
			<footer class="card-footer">
				<%= link_to "Edit", edit_user_path(@user), class: "card-footer-item button is-info" %>

				<%= link_to "Delete", user_path(@user), method: :delete, data: { confirm: "Are you sure?"}, class: "card-footer-item button is-danger" %>
			</footer>
		</div>
	</div>
</section>